import { ActionReducerMapBuilder } from '@reduxjs/toolkit';
import { HomePageState } from './common';

const homepageReducer = (
  builder: ActionReducerMapBuilder<HomePageState>,
): void => {
  builder.addCase;
};
export default homepageReducer;
